#include "AOC.h"

chrono::time_point<std::chrono::steady_clock> day14(input_t& inp) {
    int p1(0), p2(0);

    cout << inp << endl;

    auto done = chrono::steady_clock::now();
    cout << "[P1] " << p1 << "\n[P2] " << p2 << endl;
    return done;
}